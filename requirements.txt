# Balina Avcısı V9 — bağımlılıklar (Railway / pip)
# Sürümler API kırılmasına karşı sabitlendi.

python-telegram-bot>=21.0,<22.0
requests>=2.31.0,<3.0.0

# zoneinfo (Python 3.9+ stdlib) minimal Linux imajlarında tz veritabanı bulamayabilir;
# ZoneInfo("Europe/Istanbul") hatasını önlemek için tzdata şart:

tzdata>=2024.1